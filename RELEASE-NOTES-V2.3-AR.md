# Tijvorya v2.3 — Stability & Interactive Experience

## إصلاحات تشغيلية
- توحيد إعداد Cloudinary API Key مع ملف البيئة حتى يعمل توقيع الرفع عند الإعداد الصحيح.
- إضافة السحب والإفلات الحقيقي لرفع الشعار والغلاف وصور المنتجات والريلز.
- تصحيح Health API ليحدد وضع الإنتاج بناءً على اتصال Supabase الفعلي ويعرض الإصدار الصحيح.
- تقوية إنشاء الطلب: إعادة فحص حالة المنتج والمخزون والمتجر ومتجر واحد لكل طلب قبل الإنشاء.
- تحديث المخزون في واجهة التطبيق مباشرة بعد نجاح الطلب، بما في ذلك وضع التطوير المحلي.
- جعل صفحة إعدادات التاجر تحفظ الاسم والهاتف فعليًا في profile عند استخدام Supabase.
- جعل تصدير تقرير الإدارة ينشئ ملف CSV حقيقي بدل رسالة فقط.
- إزالة الحملة الإعلانية والأرقام التجريبية من صفحة الحملات؛ تبدأ الصفحة فارغة وتحفظ المسودات التي ينشئها التاجر فقط.
- إصلاح عمل زر خيارات الريلز: يمكن إخفاء ريلز من الخلاصة فعليًا، كما أصبح إعجاب التعليقات تفاعليًا.
- تجنب اختلاف hydration في حالة الريلز المحلية عبر تحميل تفضيلات المتصفح بعد التركيب.
- احترام العملة المضبوطة في المنصة في رسم المبيعات وإزالة تسمية ILS الثابتة من نموذج المنتج.

## تحسين التفاعل
- شريط تقدم للتمرير في أعلى الصفحة.
- زر عودة للأعلى يظهر عند الحاجة.
- تأثيرات دخول خفيفة للعناصر أثناء التمرير باستخدام IntersectionObserver.
- تحسين hover/focus/press للبطاقات والأزرار والحقول.
- حركة خفيفة متجاوبة للواجهة الرئيسية مع المؤشر، مع تعطيلها تلقائيًا عند Reduced Motion.
- استبدال صورة Hero الثابتة بمعاينة حية قابلة للتبديل بين المنتجات والمتاجر والريلز وتعتمد على البيانات الحقيقية فقط.
- إضافة حالات Empty State واضحة عندما لا توجد منتجات فعلية بدل ترك مساحة فارغة.

## التحقق
تم إجراء فحص Syntax لجميع ملفات TypeScript/TSX، وفحص الاستيرادات الداخلية والأصول وCSS وJSON ومتغيرات البيئة، بالإضافة إلى اختبارات منطقية لدوال الدومين والروابط والثيمات.

لا يُعتبر `npm run verify` منفذًا داخل بيئة التعديل لأن تثبيت حزم npm غير متاح في هذه البيئة؛ يجب تشغيله محليًا بعد `npm ci` للتحقق من typecheck/lint/build باستخدام الاعتماديات الفعلية.
