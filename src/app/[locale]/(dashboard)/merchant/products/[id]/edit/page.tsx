"use client";import { useParams } from "next/navigation";import { ProductForm } from "@/components/forms/product-form";import { PageHeader } from "@/components/dashboard/page-header";import { useApp } from "@/providers/app-provider";export default function Page(){const params=useParams<{id:string}>();const{locale,products}=useApp();const product=products.find(p=>p.id===params.id);return <><PageHeader eyebrow="EDIT PRODUCT" title={locale==="ar"?"تعديل المنتج":"Edit product"}/>{product?<ProductForm product={product}/>:<div className="empty-state"><h2>{locale==="ar"?"المنتج غير موجود":"Product not found"}</h2></div>}</>}