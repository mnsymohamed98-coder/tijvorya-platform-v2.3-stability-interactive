"use client";import { ProductForm } from "@/components/forms/product-form";import { PageHeader } from "@/components/dashboard/page-header";import { useApp } from "@/providers/app-provider";export default function Page(){const{locale}=useApp();return <><PageHeader eyebrow="NEW PRODUCT" title={locale==="ar"?"إضافة منتج":"Add product"} text={locale==="ar"?"أنشئ صفحة منتج كاملة مرتبطة بالمتجر والريلز.":"Create a complete product connected to your store and reels."}/><ProductForm/></>}