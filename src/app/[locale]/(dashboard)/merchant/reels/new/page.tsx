"use client";import { ReelForm } from "@/components/forms/reel-form";import { PageHeader } from "@/components/dashboard/page-header";import { useApp } from "@/providers/app-provider";export default function Page(){const{locale}=useApp();return <><PageHeader eyebrow="NEW REEL" title={locale==="ar"?"رفع ريلز":"Upload reel"} text={locale==="ar"?"اختر فيديو من الجهاز بحد أقصى 20MB واربطه بمنتج.":"Choose a device video up to 20MB and connect it to a product."}/><ReelForm/></>}